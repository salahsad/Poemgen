version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user
permissions:
  - object: /home/ec2-user
    pattern: "**"
    owner: root
    group: root
    mode: 755
hooks:
  ApplicationStart:
    - location: /start.sh
      timeout: 300
      runas: root
